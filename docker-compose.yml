services:
  consul:
    image: consul:latest
    restart: unless-stopped
    ports:
      - "8500:8500"
      - "8600:8600/udp"
    command: agent -server -ui -node=consul-server -bootstrap-expect=1 -client=0.0.0.0
    environment:
      - CONSUL_BIND_INTERFACE=eth0
    volumes:
      - consul_data:/consul/data

  app:
    image: ghcr.io/adelbeit/jamroom:${IMAGE_TAG}
    restart: unless-stopped
    environment:
      - REDIS_URL=${REDIS_URL}
      - CONSUL_HTTP_ADDR=consul:8500
    depends_on:
      redis:
        condition: service_healthy
      consul:
        condition: service_started

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD}
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  caddy:
    build:
      context: .
      dockerfile: infra/caddy-updater/Dockerfile
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile.template:/etc/caddy/Caddyfile.template:ro
      - caddy_data:/data
      - caddy_config:/config
    environment:
      - DOMAIN
      - CONSUL_HTTP_ADDR=consul:8500
      - SERVICE_NAME=jamroom-app
      - CADDYFILE_TEMPLATE=/etc/caddy/Caddyfile.template
      - CADDYFILE_PATH=/etc/caddy/Caddyfile
    depends_on:
      - consul

volumes:
  caddy_data:
  caddy_config:
  consul_data:
